const nombre = 'Nicolas';
const apellido = 'Marri';

const datosNombre = nombre + ' ' + apellido;
const nombreCompleto = `${nombre} ${apellido}`;

console.log(datosNombre)

console.log(nombreCompleto)